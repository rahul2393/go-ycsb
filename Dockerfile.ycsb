FROM alpine:3.18

RUN apk add --no-cache ca-certificates dumb-init

WORKDIR /app

# Copy the pre-built binary from the host
COPY bin/go-ycsb /app/go-ycsb

# Copy workloads configuration
COPY workloads /app/workloads

ENTRYPOINT ["/usr/bin/dumb-init", "--", "/app/go-ycsb"]
